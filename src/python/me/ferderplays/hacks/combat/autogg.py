class autogg:
    if module.enabled(module = 'autogg'):
        if mc.playerKillsEnemy(eenemy = player):
           def message():
               mc.sendChatMessage("> gg u were just killed with the power of PyHack")