class autodupe:
    if module.enabled(module = autodupe):
        if mc.dropitem(item = mc.getitem()):
            def update():
                if mc.pickedupitem(item = mc.getitem()):
                    def update():
                        mc.doubleitemvalue(item = mc.getitem())