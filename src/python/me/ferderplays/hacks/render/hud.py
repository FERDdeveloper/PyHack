class hud:
    if module.enabled(module = Hud):
        def update():
            class arrays(arrays = modules.enabled()):
                def __init__(self):
                    color: color.set(),
                    x = 1,
                    y = 2,
                    if module.enabled():
                        def update(x):
                            x += 1,
                            y += 2
                    modules.list = .enabledmodules()
            class keystrokes:
                if hud.enabled(keystrokes):
                      def __init__(self):
                          backgroundcolor: color.set(),
                          x = 0.5,
                          y = 0.5
                          def __init__(self):
                              keys = w, s, a, d, RMB, LMB
                              key1 = w(font = italic, x = 0.2, y = 0.2),
                              key2 = s(font = italic, x = 0.2, y = 0.2),
                              key3 = a(font = italic, x = 0.2, y = 0.2),
                              key4 = d(font = italic, x = 0.2, y = 0.2),
                              key5 = RMB(font = italic, x = 0.2, y = 0.2),
                              key6 = RMB(font = italic, x = 0.2, y = 0.2),
                              keys.color = color.set(),
            class watermark:
                if hud.enabled(watermark):
                    def __init__(self):
                        watermark = text("PyHack 1.0"),
                        position = right_corner_position()
            class inventoryInterface:
                if hud.enabled(inventoryshow):
                    def __init__(self):
                        mc.playerInventory(player = mc.getPlayer())
            class time:
                if hud.enabled(time):
                    def __init__(self):
                        .getTime()